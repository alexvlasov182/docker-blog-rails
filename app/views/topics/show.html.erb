<main role="main" class="container blog-margin">
  <div class="row">
    <div class="col-md-8 blog-main">
      <div class="border-bottom mb-4">
        <h1><%=	 @topic.title %></h1>
      </div>
      <%= render @blogs %>

      <%= paginate @blogs %>
    </div>
  </div>
</main>
